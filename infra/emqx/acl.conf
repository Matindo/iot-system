# EMQX ACL rules â€” topic-level authorization is handled dynamically
# via the HTTP auth webhook in emqx.conf.
# This file contains static fallback rules only.

# Allow internal bridge user full access
{allow, {user, "kafka-bridge"}, all, ["ioteca/#"]}.

# Deny everything else by default (auth webhook handles project-scoped access)
{deny, all}.
